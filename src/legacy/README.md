# Protótipo original (não montado)

Código do protótipo gerado no AI Studio (torneios, quadras, chat, placar ao vivo, H2H) com dados fictícios em `localStorage` e um seletor de "trocar usuário". Não é importado pelo app atual — ver `docs/adr/0001-backend-and-persistence.md`. Mantido como referência para portar funcionalidades para a API real.

Está fora do typecheck (`tsconfig.json` exclui `src/legacy`) e do build.
